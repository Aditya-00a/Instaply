# Templates

This folder is reserved for richer LaTeX, HTML, or ReportLab templates for:

- resume PDFs
- cover letter PDFs
- WhatsApp summary formatting references

The initial implementation uses a simple ReportLab renderer in the API layer.

